import { StepCompleted } from './StepCompleted';

describe('@reactgets/core/StepCompleted', () => {
  it('has correct displayName', () => {
    expect(StepCompleted.displayName).toStrictEqual('@reactgets/core/StepCompleted');
  });
});
